services:
    - type: web
    name: The-Crochet-Bloom-Website
    runtime: python
    rootDir: bloom_shop
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate
    startCommand: gunicorn crochet_site.wsgi:application
